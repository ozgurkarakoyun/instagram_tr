[phases.setup]
nixPkgs = ["python311", "ffmpeg"]
aptPkgs = ["fonts-dejavu-core", "fonts-liberation", "libcairo2"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python main.py"
